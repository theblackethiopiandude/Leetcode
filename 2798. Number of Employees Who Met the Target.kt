fun numberOfEmployeesWhoMetTarget(hours: IntArray, target: Int): Int {
        var ans = 0;
        for(hour in hours)
            if(hour >= target)
                ans++;
                
        return and;
}
